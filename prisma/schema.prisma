datasource db {
  provider = "sqlite"
  url      = "file:./shmaracanau.db"
}

generator client {
  provider = "prisma-client-js"
}

model ThemeConfig {
  id    Int    @id @default(autoincrement())
  theme String

  @@map("theme_config")
}
