datasource db {
  provider = "sqlite"
  url      = env("DATABASE_SQLITE_NAME")
}

generator client {
  provider = "prisma-client-js"
}

model ThemeConfig {
  id    Int    @id @default(autoincrement())
  theme String

  @@map("theme_config")
}
